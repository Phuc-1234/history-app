import { colors } from "./colors";
import { typography } from "./typography";

export const radii = {
    xs: 4,
    sm: 8,
    container: 12,
    lg: 16,
    xl: 24,
    pill: 30,
};

export const spacing = {
    xxs: 2,
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    xxl: 32,
};

export { colors, typography };
export default { colors, typography, radii, spacing };
